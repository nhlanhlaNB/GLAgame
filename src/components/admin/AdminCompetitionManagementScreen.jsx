import { useEffect, useState } from 'react'
import { styles } from '../game/gameStyles'
import { Pill, SectionHeader } from '../game/ui'
import { getCompetitionData, saveDebate, saveTeam, saveTournament } from '../../services/admin/adminCompetitionService'
import { Field, MessageCard, SimpleTable, StatusPill, inputStyle, primaryButtonStyle } from './shared/AdminFeatureUi'
function AdminCompetitionManagementScreen(){const[data,setData]=useState({teams:[],debates:[],tournaments:[]}),[team,setTeam]=useState({teamName:''}),[debate,setDebate]=useState({title:'',prompt:''}),[tournament,setTournament]=useState({title:'',maxPlayers:32}),[msg,setMsg]=useState(''),[err,setErr]=useState(''),[loading,setLoading]=useState(true);async function load(){setLoading(true);try{setData(await getCompetitionData())}catch(e){setErr(e.message)}finally{setLoading(false)}}useEffect(()=>{load()},[]);async function save(kind,e){e.preventDefault();try{if(kind==='team')await saveTeam(team);if(kind==='debate')await saveDebate(debate);if(kind==='tournament')await saveTournament(tournament);setMsg(`${kind} saved.`);await load()}catch(x){setErr(x.message)}}return <div style={styles.panel}><SectionHeader eyebrow="Team/debate/tournament admin management" title="Manage competition modes.">Create and monitor teams, debates and tournaments for multiplayer learning.</SectionHeader><MessageCard message={err} tone="error"/><MessageCard message={msg}/><div style={{display:'grid',gridTemplateColumns:'repeat(auto-fit,minmax(260px,1fr))',gap:16}}><div style={{...styles.smallCard,marginTop:18}}><p style={styles.eyebrow}>Team</p><form onSubmit={e=>save('team',e)} style={mini}><Field label="Team name"><input style={inputStyle} value={team.teamName} onChange={e=>setTeam({...team,teamName:e.target.value})}/></Field><button style={primaryButtonStyle}>Save team</button></form></div><div style={{...styles.smallCard,marginTop:18}}><p style={styles.eyebrow}>Debate</p><form onSubmit={e=>save('debate',e)} style={mini}><Field label="Title"><input style={inputStyle} value={debate.title} onChange={e=>setDebate({...debate,title:e.target.value})}/></Field><Field label="Prompt"><input style={inputStyle} value={debate.prompt} onChange={e=>setDebate({...debate,prompt:e.target.value})}/></Field><button style={primaryButtonStyle}>Save debate</button></form></div><div style={{...styles.smallCard,marginTop:18}}><p style={styles.eyebrow}>Tournament</p><form onSubmit={e=>save('tournament',e)} style={mini}><Field label="Title"><input style={inputStyle} value={tournament.title} onChange={e=>setTournament({...tournament,title:e.target.value})}/></Field><Field label="Max players"><input style={inputStyle} type="number" value={tournament.maxPlayers} onChange={e=>setTournament({...tournament,maxPlayers:e.target.value})}/></Field><button style={primaryButtonStyle}>Save tournament</button></form></div></div><div style={{...styles.smallCard,marginTop:18}}><div style={styles.rowBetween}><h3 style={styles.smallCardTitle}>Teams</h3><Pill>{data.teams.length}</Pill></div><SimpleTable loading={loading} rows={data.teams} columns={[{key:'teamName',label:'Team'}, {key:'teamStatus',label:'Status',render:r=><StatusPill value={r.teamStatus}/>}]} /></div><div style={{...styles.smallCard,marginTop:18}}><div style={styles.rowBetween}><h3 style={styles.smallCardTitle}>Debates</h3><Pill>{data.debates.length}</Pill></div><SimpleTable loading={loading} rows={data.debates} columns={[{key:'title',label:'Debate'}, {key:'prompt',label:'Prompt'}, {key:'debateStatus',label:'Status',render:r=><StatusPill value={r.debateStatus}/>}]} /></div><div style={{...styles.smallCard,marginTop:18}}><div style={styles.rowBetween}><h3 style={styles.smallCardTitle}>Tournaments</h3><Pill>{data.tournaments.length}</Pill></div><SimpleTable loading={loading} rows={data.tournaments} columns={[{key:'title',label:'Tournament'}, {key:'maxPlayers',label:'Max'}, {key:'tournamentStatus',label:'Status',render:r=><StatusPill value={r.tournamentStatus}/>}]} /></div></div>}
const mini = { display: 'grid', gap: 12 }
export default AdminCompetitionManagementScreen
